{
  "name": "popecoin_vesting",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Antony Pinedo <antonyomarpinedopuerta@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "@coral-xyz/anchor": "^0.32.1",
    "@metaplex-foundation/mpl-token-metadata": "^3.4.0",
    "@metaplex-foundation/umi": "1",
    "@metaplex-foundation/umi-bundle-defaults": "^1.5.1",
    "@solana/spl-token": "^0.4.15",
    "@solana/web3.js": "^1.99.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "ts-node": "^10.9.2",
    "typescript": "5.9.3"
  },
  "scripts": {
    "build:safe": "NO_DNA=1 anchor build --ignore-keys",
    "test:rust": "npm run build:safe && cargo test --locked",
    "check": "NO_DNA=1 anchor build --ignore-keys && cargo test --locked && tsc --noEmit && npm run test:client",
    "test:client": "node --require ts-node/register tests/run.ts",
    "verify:release": "ts-node scripts/verify-mainnet-release.ts",
    "app": "ts-node app/server.ts"
  },
  "private": true,
  "engines": {
    "node": ">=24 <25"
  },
  "resolutions": {
    "uuid": "11.1.1",
    "toml": "4.2.0"
  }
}
